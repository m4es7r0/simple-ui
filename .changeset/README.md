# Changesets

This folder contains configuration for Changesets, which manages semantic versioning and changelogs.

Useful commands:

- Record changes: `npx changeset`
- Version packages: `npm run release` (runs `changeset version && changeset publish`)


